.venv/
__pycache__/
.pytest_cache/
*.pyc
*.pyo
*.pyd
compass.db
compass.db-journal
compass.log
uploads/
notes/
.env
.compass_secret_key
.anthropic_key
.xai_key
.xai_model
.admin_emails
.sendgrid_api_key
.email_from
.app_base_url
.compass.pid
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Node / Playwright
node_modules/
test-results/
playwright-report/
mobile-screenshots/

# Test fixtures / scratch dumps
test_syllabus.pdf
*.ics

# All .txt files are local-only by default — Heroku and pip/CI need
# these specifically, so allow-list them back in.
*.txt
!requirements.txt
!requirements-dev.txt
!runtime.txt

# Claude / project documentation — local-only, never pushed.
# README.md (and any other non-Claude .md) stays tracked; add those with
# `git add -f <file>` if a new one falls under an ignored path. notes/ is
# already ignored above.
CLAUDE.md
OFFLINE-PLAN.md
docs/
extension-experimental/SPEC.md
extension-experimental/POSSIBILITIES.md
extension-experimental/SPEC-EDGE-CASES.md
extension-experimental/tests/README.md
tests_browser/MOBILE-SPEC.md
